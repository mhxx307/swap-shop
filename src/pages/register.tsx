const RegisterPage = () => {
    return (
        <div>
            <h1>RegisterPage</h1>
        </div>
    );
};

// eslint-disable-next-line react/display-name
RegisterPage.Layout = (page: any) => <>{page}</>;

export default RegisterPage;
