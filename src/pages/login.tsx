const LoginPage = () => {
    return (
        <div>
            <h1>Login</h1>
        </div>
    );
};

// eslint-disable-next-line react/display-name
LoginPage.Layout = (page: any) => <>{page}</>;

export default LoginPage;
