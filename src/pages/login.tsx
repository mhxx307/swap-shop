const Login = () => {
    return (
        <div>
            <h1>Login</h1>
        </div>
    );
};

// eslint-disable-next-line react/display-name
Login.Layout = (page: any) => <>{page}</>;

export default Login;
