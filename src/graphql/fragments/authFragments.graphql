fragment user on User {
    id
    username
    email
    address
    phoneNumber
    fullName
    birthday
    avatar
    createdDate
    updatedDate
}

fragment userMutationResponse on UserMutationResponse {
    code
    success
    message
    accessToken
    user {
        ...user
    }
    errors {
        ...errors
    }
}
