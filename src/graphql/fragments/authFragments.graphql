fragment user on User {
    id
    username
    email
    address
    phoneNumber
    fullName
    birthday
    avatar
    isOnline
    createdDate
    updatedDate
}

fragment userMutationResponse on UserMutationResponse {
    ...mutationStatus
    user {
        ...user
    }
    errors {
        ...errors
    }
}
