query Messages($conversationId: String!) {
    messages(conversationId: $conversationId) {
        id
        sender {
            username
            phoneNumber
            email
            fullName
            avatar
            id
        }
        createdDate
        text
        status
        updatedDate
    }
}
